<nav class="navbar navbar-inverse navbar-static-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main" aria-expanded="false">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ route('home') }}">{{ 'app'|trans }}</a>
        </div>
        <div class="collapse navbar-collapse" id="navbar-main">
            <ul class="nav navbar-nav">
                <li><a href="{{ route('home') }}">{{ 'nav.home'|trans }}</a></li>
            </ul>
            <div class="navbar-form navbar-left" role="search">
                <div class="form-group has-feedback">
                    <input class="form-control" type="text"  placeholder="{{ 'fields.search.placeholder'|trans }}" data-toggle="tooltip" title="{{ 'fields.search.title'|trans }}">
                    <i class="glyphicon glyphicon-search form-control-feedback"></i>
                </div>
            </div>
            {% if is_logged_in() %}
                <ul class="nav navbar-nav hidden-sm hidden-md hidden-lg">
                    <li><a href="{{ route('user_dashboard') }}">{{ 'nav.dashboard'|trans }}</a></li>
                    <li><a href="{{ route('user_profile') }}">{{ 'nav.profile'|trans }}</a></li>
                    <li><a href="{{ route('user_logout') }}">{{ 'nav.logout'|trans }}</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right hidden-xs">
                    <li class="dropdown">
                        <a href="{{ route('user_profile') }}" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            {{ 'nav.hi'|trans }} {{ user.email }} <i class="fa fa-fw fa-caret-down"></i>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ route('user_dashboard') }}">{{ 'nav.dashboard'|trans }}</a></li>
                            <li><a href="{{ route('user_profile') }}">{{ 'nav.profile'|trans }}</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ route('user_logout') }}">{{ 'nav.logout'|trans }}</a></li>
                        </ul>
                    </li>
                </ul>
            {% else %}
                <ul class="nav navbar-nav hidden-sm hidden-md hidden-lg">
                    <li><a href="{{ route('login') }}">{{ 'nav.login'|trans }}</a></li>
                </ul>
                <a class="btn btn-info navbar-btn hidden-xs" href="{{ route('login') }}">{{ 'buttons.login'|trans }}</a>
            {% endif %}
        </div>
    </div>
</nav>
